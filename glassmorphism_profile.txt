<!-- HTML Structure for a Glassmorphism Profile Card -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Glassmorphism Profile Card</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <!-- Main container for the profile card -->
    <div class="profile-card">
        <!-- Image container for the profile picture -->
        <div class="profile-image">
            <img src="profile.jpg" alt="Profile Picture">
        </div>
        <!-- Container for text details -->
        <div class="profile-details">
            <h2 class="profile-name">John Doe</h2>
            <p class="profile-bio">Web Developer | Designer | Tech Enthusiast</p>
        </div>
    </div>
</body>
</html>

<!-- CSS for the Glassmorphism Profile Card -->
<style>
    /* Universal box-sizing for consistent styling */
    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    /* Body styles for background setup */
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
        font-family: 'Arial', sans-serif;
    }
    
    /* Main card container styles */
    .profile-card {
        background: rgba(255, 255, 255, 0.15);
        border-radius: 15px;
        box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
        backdrop-filter: blur(8px);
        -webkit-backdrop-filter: blur(8px);
        border: 1px solid rgba(255, 255, 255, 0.18);
        width: 300px;
        text-align: center;
        padding: 20px;
        color: #fff;
    }

    /* Profile image container styles */
    .profile-image img {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        border: 2px solid rgba(255, 255, 255, 0.2);
        margin-bottom: 15px;
    }

    /* Profile name styles */
    .profile-name {
        font-size: 1.5em;
        margin-bottom: 10px;
    }

    /* Profile bio styles */
    .profile-bio {
        font-size: 1em;
        color: rgba(255, 255, 255, 0.8);
    }
</style>

<!-- Note: Ensure profile.jpg is present in the same directory as the HTML file for the image to display properly. -->